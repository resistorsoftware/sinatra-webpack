// This is your entry point for the home page.
// You can mount a SPA like React or VueJS

const app = document.getElementById('app');
const container = document.createElement('div');

container.innerText = 'Hello from src/home/index.js';

app.appendChild(container);

